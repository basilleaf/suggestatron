<p id="notice"><%= notice %></p>

<p>
  <b>Title:</b>
  <%= @topic.title %> - <strong><%= pluralize(@topic.votes.length, "vote") %></strong>
</p>

<p>
  <b>Description:</b>
  <%= @topic.description %> 
</p>



<%= link_to 'Edit', edit_topic_path(@topic) %> |
<%= link_to 'Back', topics_path %>
